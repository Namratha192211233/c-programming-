#include <stdio.h>
void main()
{
   int a,b;
   printf("Enter a number to find the leap year\n");
   scanf("%d",&a);
   b=a%4;
     if(b==0)
      printf("%d is leap year",a);
     else
      printf("%d is not leap year",a);
}
